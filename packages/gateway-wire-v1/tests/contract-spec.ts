import { validateToolEvent, validateUpstreamMessage } from '../src/index.ts';

export const validateToolEventSpec = validateToolEvent;
export const validateUpstreamTransportSpec = validateUpstreamMessage;
